# Module XML Calabash Application

This is the command line application that runs pipelines

# Package com.xmlcalabash.app

This package provides the command line application that runs XProc pipelines.

